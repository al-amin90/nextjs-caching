1. Fundamentals

=: what is caching? jilipi 100 user cach 

=: why caching needed in web application? 
  akta user asle ja hoi
=> user => api call => db query => data procces => page banao => user ke patao

10000 ta user akoi somoi asle ?

=: caching korar suvida ki? 
    *. performance 
    *. server load 
    *. scalability
    *. server cost kom porbe

2. Next.js 16 Caching System

=: static site redening ki? => akta boi print, akta kora holo sobai se porlo
 =>  show exjample!

==: static side render kotai use korbo?
    *. blog post
    *. marketing page
    *. documentation
    *. about us page

=: server site/Dynamic         redening? => build like static =. use > server > databae> html file
 =>  show exjample!

==: কখন ব্যবহার করবে:
    *. User-এর Dashboard (প্রত্যেকের আলাদা data)
    *. Shopping Cart
    *. Real-time stock price
    *. User profile page

static side rendering||server site rendering => 2tai kintu server side render kore 
ader pre-rendering bola hoi
ake rendering o bola hoi


=: how make a component SSR Next.js কীভাবে বোঝে Dynamic করতে হবে
    1. {cache: 'no-store'} 
    2.  const cookies = cookies();
    3.   const header = headers()
    4. searchParams                                         

=: Data Cache (App Router) @@@@@@ go to next js website and explain

3. Practical Implementation (Live Coding Required)

=: revalidate 
=> go to next js website & show console.log(), 304 , timestamps,
এটা ISR (Incremental Static Regeneration) — এক মিনিটে cache করো, পরে নতুন করো।


=: revalidateTag()
{ next: { tags: ["restaurant"] } },

=: revalidateTag()
{ next: { tags: ["restaurant"] } },

=: use cache
const getProductsData = async () => {
  "use cache";

  const res = await fetch(
    "https://fakerestaurantapi.runasp.net/api/Restaurant",
  );
  const restaurants = await res.json();

  return restaurants;
};

const nextConfig: NextConfig = {
  /* config options here */
  experimental: {
    cacheComponents: true,
  },
};

5. Common Mistakes
=: Over-caching issues? ভুল! User SPEFIC User dashboard (profile, notifications) cache করে ফেললে use dynamic rendering

=: Stale data problems? ভুল! -24 HOURS revalidate time অনেক বেশি ৫ মিনিট যথেষ্ট
=: When to use dynamic rendering? Payment page, cach allocation bNKING SYSTWM , TRANSION
=: When NOT to cache =>  ATTENEDNCE , SMS LOGS , EXAM REPORTS, AUTH LOGIN , ORDERS, COUNTERS
 Admin product price বদলালো
→ কিন্তু revalidateTag() call করা হয়নি
→ Users ঘণ্টার পর ঘণ্টা পুরনো price দেখছে
→ ব্যবসায়িক ক্ষতি! 💸

=: Cache invalidation strategy mistakes
    “Cache invalidate করা পরে দেখবো”
    Production এ:
    Cache clear করার endpoint নাই
    Tag ভুল নাম দেওয়া
    Mutiple fetch → one tag
    One fetch → multiple tags